# Abitare Marketing Dashboard — Frontend

Scaffold minimo de Next.js (App Router). Sirve `public/tracking.js` (script de
captura de UTMs/fbclid/gclid para el frontend Locomotive/Elastic externo).

El dashboard con Tremor (`/`, `/campaigns`, `/conversions`, `/roas`) esta
pendiente de implementacion en la Fase 5 — ver `docs/architecture.md` en la
raiz del repo.
